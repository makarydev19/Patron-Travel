import React from 'react';

const Destinations = () => {
  return (
    <section className="relative bg-green-900 flex items-center justify-center h-[100vh] rounded-[4rem] z-10">
      <h1 className="text-white">Destinations Cards</h1>
    </section>
  );
};

export default Destinations;
